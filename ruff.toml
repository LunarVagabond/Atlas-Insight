line-length = 100
target-version = "py311"

exclude = [
    "frontend",
    ".venv",
    "venv",
    "backend/.venv",
    "backend/apps/*/migrations",
    "__pycache__",
    "dist",
    "build",
]

[lint]
select = ["E", "F", "I", "N", "W"]
